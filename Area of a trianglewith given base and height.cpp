/* Write a program to find the area of a triangle where base and height are given */

#include<stdio.h>
#include<conio.h>
main()
{
	int b,h,area;
	b=2;
	h=4;
	area=(b*h)/2;
	printf("Area of a triangle with given base and height is %d",area);
	getch();
	
}
